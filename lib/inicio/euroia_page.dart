import 'package:flutter/material.dart';

class EuroIAPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('EuroIA'),
      ),
      body: Center(
        child: Text('Conteúdo da página de EuroIA'),
      ),
    );
  }
}
